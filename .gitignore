# Backups gerados pelos scripts de otimização
originals_backup_*
html_backup_*
backup_originals_*

# Backups de HTML
*.html.bak

# Node modules (se usar)
node_modules/

# Senha do admin (NUNCA commitar)
.env

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/
*.swp
*.swo

# Arquivos temporários
*.tmp
*.cache

# Vídeo MP4 muito grande para o GitHub (limite 100MB) - usar .webm no site
videos/vitrinni-background.mp4

# IMPORTANTE: NÃO ignorar outros arquivos de imagem/vídeo
# Precisamos dos originais (JPG/PNG) como fallback
# E também das versões otimizadas (WebP/WebM)
# Por isso NÃO há regras aqui para ignorar esses arquivos
